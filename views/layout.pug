doctype html
html
  head
      block head
        title="Express Starter App"
        link(rel='stylesheet' href='/stylesheets/reset.css')
  body
    img(src='https://cdn2.iconfinder.com/data/icons/cinema-and-music-innovicons-black-and-white/128/button-pop_corn-film_strip-cinema-movie-512.png')
    if locals.authenticated
    nav(class='navbar navbar-expand-lg navbar-dark bg-primary')
      div(class='navbar-div'): a(class='navbar-links' href='/') What to Watch
      div(class='navbar-div'): a(class='navbar-links' href='/users') My Movies
        //- for each genre in genres
        //-   a(href=genre)=genere.name
      div(class='navbar-links' id='navbarText')
        ul(class='navbar-nav mr-auto')
          //- span(class='navbar-text px-4') Welcome #{user.firstName}!
          form(class='form-inline pr-4' action='/users/logout' method='post')
            button(class='btn navbar-links' id='logout' type='submit') Logout
    //- 
      
    block content